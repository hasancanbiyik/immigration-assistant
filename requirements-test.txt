# ──────────────────────────────────────────────────────────────────────────────
# Lightweight CI test dependencies — no ML models (torch / chromadb / OPUS-MT).
# All heavy services (VectorStore, Gemini, TranslationService) are mocked in
# conftest.py so the full test suite runs in under 2 minutes on GitHub Actions.
# ──────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0

# Document parsing (lightweight, no ML)
PyMuPDF>=1.24.0
python-docx>=1.1.0
fpdf2>=2.7.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0
